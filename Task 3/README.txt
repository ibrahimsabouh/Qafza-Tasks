1. create model.py that load the iris data and create LogisticRegression model.
2. app.py the FastAPI application that loads the model and creates user interface.

run this line in the terminal to run the app:
uvicorn app:app --reload